<Window x:Class="FloorReview.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FloorReview"
             mc:Ignorable="d" 
             Closing="Window_Closing"
             d:DesignHeight="450" WindowStartupLocation="CenterScreen" Topmost="True" Width="401">
    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="dgCell" TargetType="TextBlock">
                <Setter Property="TextAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
            </Style>
            <Style x:Key="ColumnHeaderStyleC" TargetType="DataGridColumnHeader">
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="ToolTip" Value="Click to sort."/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <local:StringToBrushConverter x:Key="StringToBrushConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.12*"></RowDefinition>
            <RowDefinition Height="0.78*"></RowDefinition>
            <RowDefinition Height="0.1*"></RowDefinition>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="1" Name="list" AutoGenerateColumns="False" SelectionChanged="DataGrid_SelectionChanged" CanUserAddRows="False" ColumnHeaderStyle="{DynamicResource ColumnHeaderStyleC}">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="sd" Header="楼板" Binding="{Binding FloorName,Mode=TwoWay}" IsReadOnly="True" ElementStyle="{StaticResource dgCell}"/>
                <DataGridTextColumn Header="CAD文字信息" Binding="{Binding NoteText}" IsReadOnly="True" ElementStyle="{StaticResource dgCell}"/>
                <DataGridTextColumn x:Name="CADText"  Header="原CAD文字信息" Binding="{Binding CADText}" IsReadOnly="True" ElementStyle="{StaticResource dgCell}" Visibility="Hidden"/>
                <DataGridTextColumn Header="楼板ID" Binding="{Binding FloorID}" IsReadOnly="True" Visibility="Hidden"/>
                <DataGridTextColumn x:Name="corr" Header="是否正确" Binding="{Binding Correct}" IsReadOnly="True" Width="90" ElementStyle="{StaticResource dgCell}">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Foreground" Value="{Binding TextColor, Converter={StaticResource StringToBrushConverter}}"/>
                            <Setter Property="FontWeight" Value="Normal"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsBold}" Value="True">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                <DataGridComboBoxColumn Header="下拉框" x:Name="dataComBox" Visibility="Hidden"/>
                <DataGridTextColumn Header="标高" Binding="{Binding LevelName}"/>
                <!-- Add more columns as needed -->
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="删除所有文字注释" HorizontalAlignment="Left" Grid.Row="2" VerticalAlignment="Center" Height="43" Width="128" Click="Button_Click"/>
        <ComboBox HorizontalAlignment="Left" VerticalAlignment="Top" Width="296" Height="33" x:Name="cb" SelectionChanged="cb_SelectionChanged" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="20" Margin="44,0,0,0"/>
        <ComboBox HorizontalAlignment="Left" VerticalAlignment="Top" Width="296" Height="33" x:Name="llcb" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="20" Margin="44,44,0,0" SelectionChanged="llcb_SelectionChanged"/>
        <Button Content="文字注释赋色" HorizontalAlignment="Left" Margin="128,1,0,0" Grid.Row="2" Width="128" Height=" 43" Click="Button_Click_2"/>
        <Label Content="板厚" HorizontalAlignment="Left" Margin="4,4,0,0" VerticalAlignment="Top"/>
        <Label Content="标高" HorizontalAlignment="Left" Margin="4,48,0,0" VerticalAlignment="Top" Grid.RowSpan="2"/>
    </Grid>
</Window>
