<Window x:Class="CADPointReview.ResultWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CADPointReview"
             mc:Ignorable="d" Width="250" Height="450" Topmost="True" WindowStartupLocation="CenterScreen" Title="电气点位复核" ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="dgCell" TargetType="TextBlock">
                <Setter Property="TextAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="5"/>
            </Style>
            <Style x:Key="ColumnHeaderStyleC" TargetType="DataGridColumnHeader">
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="ToolTip" Value="Click to sort."/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.9*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" MinHeight="50" MinWidth="20" Name="list" AutoGenerateColumns="False" CanUserAddRows="False" ColumnHeaderStyle="{DynamicResource ColumnHeaderStyleC}" MouseDoubleClick="list_MouseDoubleClick" Grid.RowSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="元素ID" Binding="{Binding ElementId}" IsReadOnly="True" ElementStyle="{StaticResource dgCell}"/>
                <DataGridTextColumn Header="是否找到" Binding="{Binding Find}" IsReadOnly="True" Width="90" ElementStyle="{StaticResource dgCell}"/>
                <DataGridTextColumn Visibility="Collapsed" Header="UV坐标点" Binding="{Binding UVPoint}" IsReadOnly="True" Width="90" ElementStyle="{StaticResource dgCell}"/>
                <DataGridTextColumn Header="距地高度(mm)" Binding="{Binding PointHeight}" IsReadOnly="True" Width="90" ElementStyle="{StaticResource dgCell}"/>
                <DataGridTextColumn Header="元素类型" Binding="{Binding ElementType}" IsReadOnly="True" Width="90" ElementStyle="{StaticResource dgCell}"/>
                <!-- Add more columns as needed -->
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="距地高度计算" Grid.Row="1" Click="Button_Click"/>
    </Grid>
</Window>
