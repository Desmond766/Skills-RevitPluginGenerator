<Window x:Class="DoorReview.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DoorReview"
             mc:Ignorable="d" 
              MinHeight="400" MaxHeight="600" MinWidth="200" MaxWidth="400" Height="450" Width="300" WindowStartupLocation="CenterScreen" ResizeMode="CanResizeWithGrip" WindowStyle="None" AllowsTransparency="True" Background="#FFF0F0F0" BorderThickness="0.7" BorderBrush="Black" Topmost="True">
    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="dgCell" TargetType="TextBlock">
                <Setter Property="TextAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="5"/>
            </Style>
            <Style x:Key="ColumnHeaderStyleC" TargetType="DataGridColumnHeader">
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="ToolTip" Value="Click to sort."/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <local:StringToBrushConverter x:Key="StringToBrushConverter"/>
            <local:BrushBackColorConverter x:Key="BrushBackColorConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Grid.Row="0" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"  MouseMove="Grid_MouseMove">
            <Label Content="门复核"/>
            <StackPanel Margin="0" HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Width="30" Height="30" VerticalAlignment="Center" Cursor="Hand" ToolTip="关闭" MouseEnter="Button_MouseEnter" MouseLeave="Button_MouseLeave" Click="Button_Click_2" PreviewMouseDown="Button_PreviewMouseDown">
                    <Button.Template>
                        <ControlTemplate>
                            <Grid Background="Transparent">
                                <Line Width="10" Height="10" X1="0" Y1="0" X2="10" Y2="10" StrokeThickness="1" Stroke="Black" ></Line>
                                <Line Width="10" Height="10" X1="10" Y1="0" X2="0" Y2="10" StrokeThickness="1" Stroke="Black" ></Line>
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Name="grid_cb" Margin="15,0,15,0">
            <ComboBox DisplayMemberPath="SymbolName" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Name="cb_select_symbol" Width="{Binding ActualWidth, ElementName=grid_cb, Mode=OneWay}" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center" SelectionChanged="cb_select_symbol_SelectionChanged"/>
        </Grid>
        <DataGrid MinHeight="50" MinWidth="20" Grid.Row="2" Name="list" AutoGenerateColumns="False" CanUserAddRows="False" ColumnHeaderStyle="{DynamicResource ColumnHeaderStyleC}" MouseDoubleClick="list_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="门" Binding="{Binding DoorName}" IsReadOnly="True" ElementStyle="{StaticResource dgCell}"/>
                <DataGridTextColumn Header="CAD文字信息" Binding="{Binding NoteText}" IsReadOnly="True" ElementStyle="{StaticResource dgCell}"/>
                <DataGridTextColumn Header="门ID" Binding="{Binding DoorId}" IsReadOnly="True" Visibility="Hidden"/>
                <DataGridTextColumn Header="是否正确" Binding="{Binding Correct}" IsReadOnly="True" Width="90" ElementStyle="{StaticResource dgCell}">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Foreground" Value="{Binding TextColor, Converter={StaticResource StringToBrushConverter}}"/>
                            <Setter Property="Background" Value="{Binding TextBackColor, Converter={StaticResource BrushBackColorConverter}}"></Setter>
                            <Setter Property="FontWeight" Value="Normal"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsBold}" Value="True">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                <!-- Add more columns as needed -->
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
